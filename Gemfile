source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby "3.2.1"

gem 'okex'
gem "rufus-scheduler"
gem "dotenv"
gem "httparty"

group :development, :test do
  gem "pry"
end
