source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby "3.2.1"

gem 'okex-ruby-api', '~> 0.2.0'
gem "rufus-scheduler"
gem "dotenv"
gem "httparty"

group :development, :test do
  gem "pry"
end
